<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "users", media: "all and (max-width: 767px)" %>
    <%= stylesheet_link_tag "users_show", media: "all and (max-width: 767px)" %>
    <%= stylesheet_link_tag "posts", media: "all and (max-width: 767px)" %>
    <%= stylesheet_link_tag "posts_show", media: "all and (max-width: 767px)" %>
  </head>

  <body>
    <% if user_signed_in? %>
      <%= button_to "Logout", destroy_user_session_path, method: :delete, data: { turbo: false } %>
    <% end %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
